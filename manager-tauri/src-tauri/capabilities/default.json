{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core-shell slice: a decorated (OS-native title bar) window pointed at a broker's own /manage login. No custom commands/bridge yet -- see manager-tauri/package.json for what's deferred.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://*.vyxtrader.com/*", "http://*.localhost:3000/*"]
  },
  "permissions": ["core:default"]
}
