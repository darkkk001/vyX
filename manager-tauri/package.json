{
  "name": "vyxtrader-manager-tauri",
  "version": "0.1.1",
  "private": true,
  "description": "Tauri desktop shell for the Manager app (Broker Admin + Manager roles, app/manage/) -- core-shell slice only. Sibling to desktop-tauri/ (Client/Trading terminal) and admin-tauri/ (Super Admin), deliberately a separate app/module per project decision, not a shared codebase.",
  "scripts": {
    "build:shell": "npm --prefix manager-shell run build",
    "dev": "npm run build:shell && tauri dev",
    "build": "npm run build:shell && tauri build",
    "publish": "node publish.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
