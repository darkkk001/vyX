{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core-shell slice: a decorated (OS-native title bar) window pointed at admin.<rootDomain>/login (Super Admin). No custom commands/bridge yet -- see admin-tauri/package.json for what's deferred.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://*.vyxtrader.com/*", "http://*.localhost:3000/*"]
  },
  "permissions": ["core:default"]
}
