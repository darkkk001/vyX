{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Lets the main window's bundled local UI (admin-shell/'s built output -- see main.rs and tauri.conf.json's frontendDist) invoke the api_request/api_request_multipart commands the window.vyxDesktop bridge wraps. No `remote` block -- the window only ever loads local content now.",
  "windows": ["main"],
  "permissions": ["core:default", "allow-api-request", "allow-api-request-multipart"]
}
