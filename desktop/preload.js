// Nothing exposed yet — WebTrader is a plain web app with no need for
// Electron-specific APIs today. Kept as a hook point for later (e.g. native
// notifications for price alerts) rather than skipping contextIsolation.
