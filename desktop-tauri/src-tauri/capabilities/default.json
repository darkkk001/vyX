{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Lets the main window's own remote content (the broker's deployed WebTrader -- see main.rs) invoke the window-control commands the window.vyxDesktop bridge wraps. Scoped to this app's own domains, same allowed-host principle desktop/main.js's will-navigate handler already enforces for Electron.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://*.vyxtrader.com/*", "http://*.localhost:3000/*"]
  },
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-maximize",
    "core:window:allow-unmaximize",
    "core:window:allow-is-maximized",
    "core:window:allow-close",
    "core:event:default"
  ]
}
