{
  "name": "vyxtrader-desktop-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell -- the only desktop app (an earlier Electron app reached parity then was removed). See docs/decisions.md ADR-001.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "publish": "node publish.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
